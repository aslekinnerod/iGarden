/** Onboarding feature row: accent glyph + headline + subheadline. */
export interface FeatureRowProps {
  /** Lucide icon name */
  icon: string;
  title: string;
  text: string;
  style?: React.CSSProperties;
}