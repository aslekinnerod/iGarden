The core row of «Mine planter»: 44px thumbnail (or leaf placeholder), colored status droplet, name + "Plassering · status" caption. `PlantThumb` is exported separately for photo headers/timelines.

```jsx
<PlantRow name="Monstera" location="Stue" status="due" statusText="Vannes i dag" needsWater />
```